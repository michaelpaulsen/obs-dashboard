//the password for the websock
const OBS_PASSWORD = "password"; 
//the default scene to target when changing scene collections  
const defaultSceneName = "Minecraft"
const USE_REC_MSG = true; 
const inputname = "reason text";
export const skc_settings = {
    OBS_PASSWORD,
    defaultSceneName,
    USE_REC_MSG,
    inputname
}; 

//a function for connecting to obs
